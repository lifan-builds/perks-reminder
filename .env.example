# Perks Reminder environment template

# Database
DATABASE_URL=
DATABASE_URL_DEV=

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Services
RESEND_API_KEY=
CRON_SECRET=
SERPAPI_API_KEY=

# Analytics & SEO
GOOGLE_ANALYTICS_ID=
GOOGLE_SEARCH_CONSOLE_VERIFICATION=

# Optional
# NODE_ENV=development
# Authentication
# Get these from Google Cloud Console OAuth 2.0 credentials
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-your-google-client-secret"

# NextAuth Configuration
# URL where your app is running (localhost for development, your domain for production)
NEXTAUTH_URL="http://localhost:3000"
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-key-here"

# SerpAPI for search functionality (optional)
# Get from https://serpapi.com/
SERPAPI_API_KEY="your-serpapi-key-here"

# Database Configuration
# Production database URL (Neon/PostgreSQL) — use the POOLER endpoint
# Note: connection_limit and pool_timeout prevent connection exhaustion in serverless environments
# sslmode=require ensures secure connections
DATABASE_URL="postgresql://username:password@your-database-host-pooler.neon.tech/dbname?sslmode=require&connection_limit=10&pool_timeout=20"
# Direct (non-pooler) connection for migrations — required for advisory locks
DIRECT_URL="postgresql://username:password@your-database-host.neon.tech/dbname?sslmode=require"
# Development database URL (Neon branch or separate database)
DATABASE_URL_DEV="postgresql://username:password@your-dev-database-host.neon.tech/dbname?sslmode=require&connection_limit=5&pool_timeout=20"


# Cron Job Security
# Generate with: openssl rand -hex 32
CRON_SECRET="your-cron-secret-key-here"

# Email Service (Resend)
# Get from https://resend.com/
RESEND_API_KEY="re_your-resend-api-key-here"
FROM_EMAIL="YourApp <notifications@yourdomain.com>" 