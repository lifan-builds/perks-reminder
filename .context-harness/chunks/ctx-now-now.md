# Now

# Now

## Current Focus
Implemented, deployed, and production-verified the public Neon quota hardening plan: anonymous public catalog/guide/referral/sitemap surfaces now use a static shared catalog instead of Prisma, deploy builds no longer run `prisma db seed`, and public DB-free invariants/build checks are in place. Applied the safe Neon quota-prevention setting available on Free: production project defaults and all existing endpoints are now `0.25-1 CU`. Supabase fallback is documented but not cut over. Current work is provider recovery for DB-backed logged-in features.

## Active Blockers
- Neon production and dev databases are currently unavailable due to provider compute quota exhaustion: `ERROR: Your account or project has exceeded the compute time quota. Upgrade your plan to increase limits.` Code can degrade more gracefully, but DB-backed app features will stay unavailable until the Neon quota is restored, upgraded, or reset.
- Neon console is now logged in. Org `Lifan Free` shows project `credit-card-tracker` (`old-waterfall-78542145`) at `110.31 / 100 CU-hrs` for June 2026 with the banner `You've used all of your monthly compute allowance for this project.` Branch usage shows `production` consumed `109.8 CU-hrs`, `development` consumed `0.51 CU-hrs`, and old backup/staging branches consumed `0 CU-hrs`; all visible computes are already idle/archived, so branch cleanup will not reclaim the already-spent June quota.
- Neon project `old-waterfall-78542145` has been safely quota-hardened for recurrence prevention: `default_endpoint_settings` now use `autoscaling_limit_min_cu: 0.25` and `autoscaling_limit_max_cu: 1`, and all six endpoint records now report `min_cu: 0.25`, `max_cu: 1`, and `current_state: idle`. The Neon API rejected explicit suspend interval edits on this Free account with `modifying the suspend interval is not permitted on this account`; according to the installed Neon API types, `suspend_timeout_seconds: 0` means use the default interval and `-1` means never suspend.
- Billing page shows Free plan includes `100 compute hours`; visible recovery path is paid `Launch` at `$0.106 per CU-hour` with free limits removed. Do not select/confirm a paid plan without explicit user approval.
- Supabase fallback registration has started with `fantasychen2016@gmail.com`; the sign-up form is blocked at an hCaptcha challenge. Browser safety requires user confirmation before the agent solves a CAPTCHA, or the user can solve it manually in Chrome and say when it is done.
- Existing Neon project `credit-card-tracker-staging` (`small-base-05262044`) is available as a no-paid fallback target with `0 / 100 CU-hrs` used in June 2026. Its `production` branch connection string was copied for verification and then cleared from the local clipboard; re-copy it from Neon if needed, and do not print or persist it.
- The staging fallback DB is prepared and verified: historical pre-init migrations were baselined on that fallback only, current migrations were applied, skipped index/search SQL was executed manually, `npx prisma db push` synced schema drift, `npm run db:seed` seeded 34 predefined cards, 129 predefined benefits, 34 usage ways, and 24 loyalty programs, and `npm run db:check` reports schema up to date against the fallback.
- Fallback smoke result: local server on `http://localhost:3004` against staging DB created a throwaway credentials user, signed in, added American Express Business Gold Card, materialized 2 benefit statuses, loaded `/cards`, `/`, `/settings/data`, `/loyalty`, and served `/api/user-cards/export` with 200. The fallback DB has no real production users/cards; cutover would create a temporary empty-account experience for existing users.
- Searched likely local dump/backup locations under the repo, `/private/tmp`, `Downloads`, `Desktop`, `Documents`, and `/Users/lfan` filename patterns; no production SQL/pgdump backup was found.
- Neon CLI is authenticated and can read both projects. `neon branches list --project-id old-waterfall-78542145` shows all real production project branches are archived; the CLI supports restore/reset only inside the same project, so it does not provide a no-paid cross-project data-copy path from exhausted production into staging.
- Neon API/CLI inspection found branch clone/restore is project-local and project creation cannot initialize from a branch in another project. Without a live production compute connection or a prior dump, a new database cannot preserve existing production user data.
- Vercel CLI access is confirmed. Production domains are on Vercel project `coupon-cycle` (`prj_x0VAgiuO3PAem4IUmeVo9dsRMCsx`) in scope `lifan-chens-projects`; production env names include `DATABASE_URL` and `DIRECT_URL`. Use a temporary `.vercel/project.json` outside the repo if targeting `coupon-cycle`, because this checkout is linked to `credit-card-tracker`.
- Local checks still show the non-blocking Next SWC version warning (`@next/swc` 15.5.7 detected while Next.js is 15.5.11).
- `npm run db:check` now executes again, but Neon prod/dev still report migration status errors while quota/provider access is unavailable.

## Immediate Next Step
User chose to wait for the July 2026 Neon quota reset instead of upgrading Neon or cutting over to an empty fallback. Expected reset boundary is `2026-07-01T00:00:00Z` / June 30, 2026 5:00 PM PDT. After reset, immediately run `npm run db:check`, verify logged-in production DB-backed flows, export a Neon backup, and monitor July usage with the public DB-free hardening in place. Do not point production at the empty fallback DB or click a paid plan without fresh explicit approval.

## Session State
- Last modified: 2026-06-26T21:25:43Z
- Touched files: `CONTEXT.md`, `NOW.md`, `package.json`, `next.config.ts`, `prisma/seed.ts`, `docs/supabase-fallback.md`, `scripts/check-database-connection.js`, `scripts/check-public-db-invariant.cjs`, `scripts/check-public-build-without-db.cjs`, `src/lib/static-catalog.ts`, `src/lib/cardSearchUtils.ts`, `src/lib/home-dashboard-data.ts`, `src/lib/__tests__/static-catalog.test.ts`, `src/app/page.tsx`, `src/app/cards/browse/page.tsx`, `src/app/cards/browse/[name]/page.tsx`, `src/app/cards/new/page.tsx`, `src/app/api/predefined-cards/route.ts`, `src/app/api/predefined-cards-with-benefits/route.ts`, `src/app/api/search/route.ts`, `src/app/benefits/how-to-use/page.tsx`, `src/app/benefits/how-to-use/[slug]/page.tsx`, `src/app/referrals/page.tsx`, `src/app/sitemap.ts`, `src/components/SupportedCreditCards.tsx`, `src/components/__tests__/SupportedCreditCards.test.tsx`, `src/middleware.ts`.
- Verification: `npm run check:public-db`, `npx tsc --noEmit --pretty false`, `npm test -- static-catalog SupportedCreditCards`, `npm run check:public-build:no-db`, `npm run build` (passes; migration deploy skipped due Neon schema engine error), local production server with intentionally unreachable DB URLs, direct `curl` 200 checks for `/`, `/cards/browse/American%20Express%20Gold%20Card`, `/benefits/how-to-use/airline-fee-credits`, `/referrals`, `/sitemap.xml`, `/api/predefined-cards`, `/api/predefined-cards-with-benefits`, and `/api/search?q=gold`, targeted HTML assertions for homepage/card content with no generic error text, and in-app browser render of the direct card page showing `American Express Gold Card` with no generic error boundary. Pushed `main` through commits `08101f9` and `8a1db96`; latest Vercel `coupon-cycle` deployment `coupon-cycle-c4ue3vn0d-lifan-chens-projects.vercel.app` became Ready and aliases `perks-reminder.com` / `www.perks-reminder.com`. Final production direct checks return 200 for `https://www.perks-reminder.com/cards/browse/American%20Express%20Gold%20Card`, apex card URL, `/benefits/how-to-use/airline-fee-credits`, and `/api/search?q=gold`; production content assertions confirm expected card/guide text, no generic/internal error text, and search `metadata.type` is `static`. `vercel logs` was attached during fresh public requests and emitted no runtime errors. Neon API readbacks after quota-prevention update confirm project defaults and all six endpoints are `0.25-1 CU`, all endpoints are idle, and `suspend_timeout_seconds` remains `0` because explicit suspend interval edits are blocked on the Free account. `npm run db:check` was rerun after the settings change and still reports production/development connection status errors, confirming the already-spent June quota remains the active DB-backed-feature blocker.
